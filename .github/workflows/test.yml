name: CI

on:
  push:
    branches: [ "master" ]

env:
  full_name: "cao hai nhi"

jobs:
  job1:
    runs-on: ubuntu-latest
    env:
      age: 27
    steps:
      - name: Change name
        id: change-name
        run: |
          # echo My name is $full_name
          echo "::set-env name=full_name::Chandrasekara"

  job2:
    runs-on: ubuntu-latest
    env:
      favorite_dish: "spring rolls"
    steps:
      - run: |
          echo "The name was changed to $full_name"
